import {Story} from "@storybook/react";
import React from 'react'
import { getConfig } from '@scripts/storybook/storyConfig'
import {{ pascalCase componentName }}, { I{{ pascalCase componentName }}Props } from './{{ pascalCase componentName }}'

export default getConfig({ title: 'atoms/{{ pascalCase componentName }}', component: {{ pascalCase componentName }} })

const Template: Story<I{{ pascalCase componentName }}Props> = (args: I{{ pascalCase componentName }}Props) => <{{ pascalCase componentName }} {...args}></{{ pascalCase componentName }}>;

export const Common = Template.bind({});
Common.args = {};
import PropTypes from 'prop-types'
import React from 'react'
import './{{ pascalCase componentName }}.scss'

export interface I{{ pascalCase componentName }}Props {}

function {{ pascalCase componentName }}({}: I{{ pascalCase componentName }}Props) {
  return <div className={'molecule__{{ dashCase componentName}}'}>{{componentName}}</div>
}

{{ pascalCase componentName }}.propTypes = {
  text: PropTypes.string
}

export default {{ pascalCase componentName }}
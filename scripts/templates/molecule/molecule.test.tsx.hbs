/* eslint-disable no-console */
import { render } from '@testing-library/react'
import React from 'react'
import ReactDOM from 'react-dom'
import {{ pascalCase componentName }} from './{{ pascalCase componentName }}'

describe('{{ pascalCase componentName }}', () => {
  test('renders without crashing', () => {
    const div = document.createElement('div')
    ReactDOM.render(<{{ pascalCase componentName }} />, div)
    ReactDOM.unmountComponentAtNode(div)
  })
  test('Find component with text {{ pascalCase componentName }}', () => {
    const { getByText } = render(
    <{{ pascalCase componentName }} />)
    expect(getByText('{{componentName}}'))
  })
})